_build/
.env
.vscode

# Signer keys persisted by the rotate-signer CLI command (see lib/setup/signer.mjs)
.signer-state.json

# Solver card generated per start for lnurl-server (see lib/setup/lnurl.mjs)
.lnurl-solver-cards.json
